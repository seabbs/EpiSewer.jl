# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Test

on:
  push:
    branches:
      - main
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: EpiAware/.github/.github/workflows/tests.yml@2c7ce73459772f11dad46ac7f456e35f0d296faf
    with:
      # The matrix this package tests on. Seeded, not managed: name your own
      # value here and a resync keeps it (#73, #117). lts (Julia 1.10) is
      # dropped because the dependency chain (ComposableTuringIDModels,
      # EpiAwareADTools 0.2) floors at Julia 1.11; the jet/ad/docs/benchmark
      # environments that need the [sources] pin are separate jobs on the
      # current release.
      julia_versions: '["1", "pre"]'
    secrets: inherit  # pragma: allowlist secret

  downgrade-compat:
    uses: EpiAware/.github/.github/workflows/downgrade.yml@2c7ce73459772f11dad46ac7f456e35f0d296faf
    with:
      julia_version: '1'
    secrets: inherit  # pragma: allowlist secret
