import{_ as t,o,c as a,an as i}from"./chunks/framework.DVHqmV-v.js";const m=JSON.parse('{"title":"LLM-Assisted Development Process","description":"","frontmatter":{},"headers":[],"relativePath":"components/llm-process.md","filePath":"components/llm-process.md","lastUpdated":null}'),s={name:"components/llm-process.md"};function n(r,e,l,d,h,c){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="LLM-Assisted-Development-Process" tabindex="-1">LLM-Assisted Development Process <a class="header-anchor" href="#LLM-Assisted-Development-Process" aria-label="Permalink to &quot;LLM-Assisted Development Process {#LLM-Assisted-Development-Process}&quot;">​</a></h1><h2 id="Aim" tabindex="-1">Aim <a class="header-anchor" href="#Aim" aria-label="Permalink to &quot;Aim {#Aim}&quot;">​</a></h2><p>This package is an experiment to see if non-frontier models can be used to effectively port R packages to Julia when using composable elements built by frontier models.</p><h2 id="Methodology" tabindex="-1">Methodology <a class="header-anchor" href="#Methodology" aria-label="Permalink to &quot;Methodology {#Methodology}&quot;">​</a></h2><ul><li><p><strong>Code harness</strong>: pi coding agent with subagents (pi-subagents)</p></li><li><p><strong>Language model</strong>: DeepSeek V4 Flash via pi-agent (openrouter/~deepseek/deepseek-v4-flash-latest)</p></li><li><p><strong>Date</strong>: August 2026</p></li><li><p><strong>Cost</strong>: $13.62 on OpenRouter, part of an $18.82 total across both non-frontier passes.</p></li></ul><h2 id="Review-pass" tabindex="-1">Review pass <a class="header-anchor" href="#Review-pass" aria-label="Permalink to &quot;Review pass {#Review-pass}&quot;">​</a></h2><ul><li><p><strong>Date</strong>: 2026-08-15. <strong>Harness</strong>: pi coding agent with pi-subagents (same as the implementation pass).</p></li><li><p><strong>Review model</strong>: Kimi 3 as the overseer, delegating to DeepSeek V4 Flash subagents (openrouter/~deepseek/deepseek-v4-flash-latest) as the weak workers. Both non-frontier, so the experiment covers non-frontier review of non-frontier work.</p></li><li><p><strong>Cost</strong>: $5.20 on OpenRouter, the remainder of the $18.82 total.</p></li><li><p><strong>Instructions</strong>: review each section of the replication prompt against the repository state; get CI green (quality suite, JET, AD, Documenter); correct drift from the EpiAwarePackageTools README standard; make the README worked example runnable code for the Documenter pipeline rather than saved plots and scripts; align the example data with the original EpiSewer example (sparse Monday/Thursday measurements over the same window); enforce ecosystem reuse over custom code; keep code and comments concise and elegant; work through heavy weak-worker subagent delegation with the review model as overseer, committing and pushing each fix.</p></li></ul><p><strong>Operator bumps</strong>: the implementation agent stopped work twice and had to be bumped by the operator — (1) after component development, leaving empty placeholder modules, an outdated model-components table, and the examples section unstarted (operator sent a &quot;keep-going&quot; prompt); (2) at the final checkpoint, stopping with CI still red and asking whether to continue.</p><h2 id="Frontier-model-review-pass" tabindex="-1">Frontier-model review pass <a class="header-anchor" href="#Frontier-model-review-pass" aria-label="Permalink to &quot;Frontier-model review pass {#Frontier-model-review-pass}&quot;">​</a></h2><p>The experiment&#39;s premise is that a non-frontier model can port a package when the composable pieces are supplied by frontier models. Testing that premise needs a frontier model to check the result, so a third pass audited the two non-frontier passes above.</p><ul><li><p><strong>Date</strong>: 2026-08-17.</p></li><li><p><strong>Harness</strong>: Claude Code 2.1.233 (CLI), main agent plus <code>Task</code> subagents.</p></li><li><p><strong>Review model</strong>: Claude Opus 5 (1M-token context) for the main agent and every subagent.</p></li><li><p><strong>Scale</strong>: one main agent plus 11 subagents; ~3,200 model calls, ~1.4 million output tokens and ~834 million input-side tokens (almost all of it prompt-cache reads), measured from the session transcripts.</p></li><li><p><strong>Cost</strong>: $Z (placeholder — to be filled in once the project is complete).</p></li><li><p><strong>Instructions</strong>: work through every bullet of the replication prompt and record its true status against the repository rather than against the previous passes&#39; own session notes; fix the failing Enzyme AD CI jobs; and judge how closely the package tracks <code>ComposableTuringIDModels.jl</code> in style and in reuse.</p></li><li><p><strong>Output</strong>: 24 commits, 10 issues filed on this repository and 5 upstream across three ecosystem packages.</p></li></ul><p>The scale ratio is the part worth recording. Auditing the port cost roughly two orders of magnitude more inference than producing it: the two non-frontier passes together came to $18.82, and this pass consumed ~834 million input-side tokens against their output. That is not a like-for-like comparison — reading a codebase to check it is inherently heavier than writing it, and prompt-cache reads are cheap per token — but it does undercut a tempting reading of the experiment. If a cheap model ports a package and an expensive one is needed to find out whether the port is right, the saving is smaller than the model prices suggest.</p><h3 id="What-the-review-found" tabindex="-1">What the review found <a class="header-anchor" href="#What-the-review-found" aria-label="Permalink to &quot;What the review found {#What-the-review-found}&quot;">​</a></h3><p>Most bullets were delivered, and the ecosystem carried the port. Five new structs (<code>LOD</code>, <code>LogNormalError</code>, <code>DigitalPCRError</code>, <code>MeasurementOutliers</code>, <code>FlowNormalize</code>) cover a model whose R original is assembled from about twenty components; everything else is composition of existing <code>ComposableTuringIDModels.jl</code> pieces. The package built, its tests passed, its documentation read plausibly, and it produced a fitted <code>R_t</code> in a believable range.</p><p>It was also wrong in four ways that mattered, none of which any test caught. 2. <strong>The generation interval was indexed one lag too early.</strong> Its values matched R&#39;s <code>get_discrete_gamma_shifted</code> to four decimal places, but R&#39;s formula runs <code>k &lt;- 1:maxX</code>, so R&#39;s first bin is lag 1 while ours was lag 0. The model therefore allowed 28.7% of transmission to happen on the day of infection, and its mean generation interval was 1.96 days rather than the 2.94 the parameterisation asks for. A generation interval a day too short compresses estimated <code>R_t</code> toward 1.</p><ol start="3"><li><p><strong>The incubation-period convolution was missing entirely.</strong> It was computed and never used. Because the shedding load is indexed from symptom onset, omitting it applied the whole shedding profile about 3.4 days early. The README explains why the incubation period is needed, and then the model did not use it.</p></li><li><p><strong>Thirty-one per cent of the observations were never scored.</strong> Each delay convolution shortens the expected series, and the observation loop aligns to its end, so the first 37 of 120 measurements contributed nothing to the likelihood. Setting the first observation to an absurd value left the log-joint bit-identical. The returned series was still full length, which is what hid it.</p></li><li><p><strong>The worked example fitted the wrong dataset.</strong> EpiSewer&#39;s README example deliberately thins its measurements to Mondays and Thursdays and then shows the fit recovering the withheld days. Ours fitted 117 of 120 days, so the missing-data behaviour the example exists to demonstrate was exercised on three days instead of eighty-six.</p></li></ol><p>The pattern is consistent, and it is the useful result of the experiment. Every one of these is a composition error or an off-by-one in an index, not a mistake inside a component. Each component was individually correct and individually tested. The errors live in how the pieces were joined and in what the numbers meant, which is exactly what a unit test of a component cannot see.</p><p>Two further findings concern how the work was verified rather than what it produced.</p><p><strong>The test suite included a whole category of tests that could not fail.</strong> Every component had an automatic-differentiation scenario, and no scenario called the component: each re-implemented the component&#39;s log-density inside the fixture file. A seven-backend AD matrix was reporting green over code the package never executed. Rewiring those scenarios to differentiate the real components found a genuine bug within minutes — a <code>hasmethod</code> check reachable on the differentiated path, which Mooncake has no rule for — that the previous scenarios could not have detected, because the closure they differentiated contained none of the code at fault.</p><p>The same pattern held in the unit tests, and there it was countable. Seven test items asserted only that the code had run: <code>mdl !== nothing</code>, or that the sampler returned the number of draws it was asked for. One of them was self-documenting. <code>test/sewage.jl</code> asserted <code>mdl !== nothing</code> with a comment saying the flow division&#39;s correctness was &quot;covered by the integration tests&quot;, and the integration test it pointed at asserted <code>mdl !== nothing</code>. Two tests each deferring to the other, so flow normalisation — the package&#39;s central transformation — had no assertion anywhere in the suite. That is not a gap someone forgot to fill; it is a gap that documented itself as filled.</p><p>Clearing all seven took the suite from 194 assertions to 192, because the replacements were more precise rather than more numerous.</p><p>Two of the six test files contained no such tests at all, and the reason generalises past this project. <code>test/data.jl</code> and <code>test/distributions.jl</code> check specific counts, dates, types, and numbers computed from the R original — the mean generation interval against a transcription of R&#39;s own discretiser, the shedding mode, the window boundaries. An assertion whose expected value has to come from outside the code under test cannot be vacuous. In every instance found here, the smoke was in a test whose expected value came from the model itself, or from nothing at all.</p><p><strong>Infrastructure consumed the most effort and produced the longest-lived failure.</strong> The Enzyme jobs stayed red for two days over a missing <code>function_annotation = Enzyme.Const</code> on two backend constructors, a one-line annotation that <code>ComposableTuringIDModels.jl</code> already carries with the reason written beside it.</p><h3 id="What-the-review-had-to-change" tabindex="-1">What the review had to change <a class="header-anchor" href="#What-the-review-had-to-change" aria-label="Permalink to &quot;What the review had to change {#What-the-review-had-to-change}&quot;">​</a></h3><p>The audit was not read-only. Nine defects were fixed in the same pass, in 24 commits.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Defect</th><th style="text-align:right;">Effect before the fix</th></tr></thead><tbody><tr><td style="text-align:right;">Generation interval indexed one lag early</td><td style="text-align:right;">mean 1.96 days instead of 2.94, and 28.7% of transmission on the day of infection</td></tr><tr><td style="text-align:right;">Incubation convolution absent</td><td style="text-align:right;">the whole shedding profile applied ~3.4 days early</td></tr><tr><td style="text-align:right;">Lead-in not accounted for in <code>n</code></td><td style="text-align:right;">44 of 120 observations never scored</td></tr><tr><td style="text-align:right;">Worked example on the dense series</td><td style="text-align:right;">the missing-data demonstration exercised on 3 days instead of 86</td></tr><tr><td style="text-align:right;"><code>MeasurementOutliers</code> a contamination mixture</td><td style="text-align:right;">a different model from <code>outliers_estimate</code></td></tr><tr><td style="text-align:right;">Spike prior untruncated</td><td style="text-align:right;">36.8% of prior mass on negative spikes Stan forbids</td></tr><tr><td style="text-align:right;"><code>DigitalPCRError</code> data path</td><td style="text-align:right;">a <code>missing</code> count frozen at the first draw and scored as data</td></tr><tr><td style="text-align:right;"><code>hasmethod</code> on the differentiated path</td><td style="text-align:right;">Mooncake reverse failed; other backends silently tolerated it</td></tr><tr><td style="text-align:right;">Enzyme backends missing <code>function_annotation</code></td><td style="text-align:right;">two AD jobs red for two days</td></tr></tbody></table><p>Alongside those: four unused dependencies removed from the package (one of them pulling the whole Makie stack), the AD gradient tolerance tightened from the kit&#39;s default by five orders of magnitude, seven vacuous test items cleared, nine dead documentation links fixed, and five table rows corrected that described designs the code had replaced.</p><p>Two things could not be fixed from here and are recorded as issues: the <code>Auto Version Increment</code> workflow needs a repository setting only a maintainer can change, and the coverage upload needs a token. One more is left open deliberately — the worked example&#39;s plots have not been regenerated, because the fit they came from used a 60-draw fallback on the wrong data, and replacing them with a fit nobody has verified would repeat the mistake being corrected.</p><h3 id="What-the-review-got-wrong" tabindex="-1">What the review got wrong <a class="header-anchor" href="#What-the-review-got-wrong" aria-label="Permalink to &quot;What the review got wrong {#What-the-review-got-wrong}&quot;">​</a></h3><p>Two of the gaps this pass reported upstream as missing ecosystem capabilities were not missing. Stochastic infection noise and an estimated seeding phase are both reachable through documented, <code>public</code> extension points — a renewal modifier&#39;s returned incidence re-enters the recursion, and the initialisation window can be supplied by a custom step. Both issues were retracted after the operator questioned the first one. The lesson generalises past this project: an audit that concludes a capability is absent should trace the extension seams before saying so, and a reviewer is as capable of a confident wrong answer as the work under review.</p><p>The full bullet-by-bullet audit is kept with the other review notes in the gitignored <code>.resources/</code> directory.</p><h2 id="Source-material" tabindex="-1">Source material <a class="header-anchor" href="#Source-material" aria-label="Permalink to &quot;Source material {#Source-material}&quot;">​</a></h2><p>The original EpiSewer R package source code was embedded as a resource at <code>.resources/EpiSewer/</code>.</p><h2 id="The-replication-prompt" tabindex="-1">The replication prompt <a class="header-anchor" href="#The-replication-prompt" aria-label="Permalink to &quot;The replication prompt {#The-replication-prompt}&quot;">​</a></h2><p>The prompt that guided this project is reproduced in full below, as prose rather than as a code block. It lives on this page because the original <code>.resources/prompt.md</code> is not committed (<code>.resources</code> is gitignored). Only three things have changed: the heading levels are shifted to sit under this section, one unbalanced inline-code marker is closed, and the cost placeholder&#39;s dollar sign is escaped so it is not read as maths. The wording, ordering and typographical errors are exactly as the implementing agents received them.</p><h3 id="Replication-plan-for-EpiSewer" tabindex="-1">Replication plan for EpiSewer <a class="header-anchor" href="#Replication-plan-for-EpiSewer" aria-label="Permalink to &quot;Replication plan for EpiSewer {#Replication-plan-for-EpiSewer}&quot;">​</a></h3><h4 id="Aim-2" tabindex="-1">Aim <a class="header-anchor" href="#Aim-2" aria-label="Permalink to &quot;Aim {#Aim-2}&quot;">​</a></h4><p>I want to replicate the model functionality but not interface or other tools (i.e. plotting, docker etc) of the EpiSewer package using tools from the EpiAware.org GitHub Ecosystem</p><h4 id="Setup" tabindex="-1">Setup <a class="header-anchor" href="#Setup" aria-label="Permalink to &quot;Setup {#Setup}&quot;">​</a></h4><ul><li><p>We want to first make sure we can install and run <code>EpiSewer</code></p></li><li><p>We will first set the Package up using <code>EpiAwarePackageTools.jl</code>.</p></li><li><p>Add <code>.resources</code> to the <code>.gitignore</code></p></li><li><p>Our core tools and dependencies will be <code>ComposableTuringIDModels.jl</code>, <code>CensoredDistributions.jl</code>, <code>EpiAwareADTools.jl</code>. This is on top of the tools package tools will bring.</p></li><li><p>We know there are issues declaring as not part of the EpiAware.org ecosystem so we may need to make a few tweaks so that the docs show up in our samabbott.co.uk/EpiSewer.jl.</p></li><li><p>We need to opt in to AD benchmarking</p></li><li><p>We will want to flesh out what this package is i.e its readme folling EpiAware standards</p></li><li><p>We need to add a clear derived from EpiSewer attribution</p></li><li><p>We will want to make a documentation page that details the LLM process used to create the package. It should have this document in it as i.e. the prompt. It should start with the aim (experimenting to see if non-frontier models can be used to effectively language port packages when using composable elements built by frontier models). You should record in this document the code harness we are using as well the as the LLM model used and the date. We will also want cost here but for that leave placeholder i.e $X and I will add it once the project is done.</p></li><li><p>We need to also note that we had the <code>EpiSewer</code> source code embedded as a resource.</p></li></ul><h4 id="Documentation-driven-development" tabindex="-1">Documentation driven development <a class="header-anchor" href="#Documentation-driven-development" aria-label="Permalink to &quot;Documentation driven development {#Documentation-driven-development}&quot;">​</a></h4><ul><li><p>We want to replicate the modelling in the readme of the package in our readme</p></li><li><p>To begin this process we want to have text without code i.e Introduction is our example for the readme (not the checksums)</p></li><li><p>To get ready for modelling we want to get the example data EpiSewer uses into our Julia package in a format we can load it in from (<code>ComposableTuringModels.jl</code> has examples of this).</p></li><li><p>We will also want to add packages i.e <code>DataFramesMeta.jl</code> and <code>AlgebraOfGraphics.jl</code> that we will need for manipulating data and plotting it (as well as <code>PairsPlot.jl</code>).</p></li><li><p>We then want to make a page in the documentation of the package (perhaps) the overview tutorial section or similar) that has a table of all the model components that EpiSewer has (I imagine giving them their R names but these will all be things with stan functions)</p></li><li><p>We then want to explore <code>ComposableTuringIDModels.jl</code> and find the components it has that match elements of the <code>EpiSewer</code> model</p></li><li><p>We now want to make github issues for the components that don&#39;t have counter parts in the <code>EpiAware.jl</code> ecosystem. These should be small modules that fit the <code>ComposableTuringIDModels.jl</code> patterns.</p></li><li><p>Note that we don&#39;t need the custom discretisation tools Adrian uses here as we have those in <code>CensoredDistributions.jl</code> to make use of.</p></li></ul><h4 id="Component-development" tabindex="-1">Component development <a class="header-anchor" href="#Component-development" aria-label="Permalink to &quot;Component development {#Component-development}&quot;">​</a></h4><ul><li><p>For each of the small components we should implement it as a <code>ComposableTuringIDModels.jl</code> compatible <code>Struct</code>. We to have unit tests for these which test against functionality. We will also want to add them to our AD scenario testing suite. As implementing it is imperative to run the tests and confirm i.e AD support works.</p></li><li><p>We should also have some small integration unit tests that test our new components in simple <code>ComposableTuringIDModels.jl</code></p></li><li><p>Make sure to sense check the new components against the the <code>EpiSewer</code> target components where possible. Usually it is best to do this by writing small scratch stan models (potentially saving the as <code>.gitignore</code> files in <code>.resouces</code> for safe keeping.). You can use generated quantities to test the eval. Generally we shoulnd&#39;t need to test gradients against each other here.</p></li></ul><h4 id="Implementing-the-examples" tabindex="-1">Implementing the examples <a class="header-anchor" href="#Implementing-the-examples" aria-label="Permalink to &quot;Implementing the examples {#Implementing-the-examples}&quot;">​</a></h4><ul><li><p>We should now be able to implement the <code>EpiSewer</code> README modelling example. We want to use the style of composable model building used in the <code>ComposableTuringIDModels.jl</code> where we compose components with that ecosystem with the new ones we have made in this package cleanly.</p></li><li><p>To confirm this model works we should then fit it to <code>EpiSewer</code> data - upgrading the NUTs settings as needed to ensure a clean and convergent fit (use 2 chains and 2 cores).</p></li><li><p>We can then make each of the plots in the <code>EpiSewer</code> readme using our new model.</p></li><li><p>We also want add a <code>PairsPlot.jl</code> that shows both the posterior and the prior densities for key parameters (likely not the latent model over time parameters for brevity).</p></li></ul><h4 id="Development-process" tabindex="-1">Development process <a class="header-anchor" href="#Development-process" aria-label="Permalink to &quot;Development process {#Development-process}&quot;">​</a></h4><ul><li><p>We want to make heavy use of subagents with the main agent just being a dispatcher into subagents.</p></li><li><p>Generally we want to dispatch and agent to do a task (often one of these bullets), then another agent to review it against that bullet and earlier bullets.</p></li><li><p>You should repeat this process until the review agent is happy. It is key that package tests are passing at all times and precommit is clean locally.</p></li><li><p>Each agent should commit their task as a single commit (for those responding to the review including the correction points). It is fine to commit to main for this project.</p></li><li><p>After each subtitle section is complete dispatch a review agent to double check the work from that section is complete and that CI is passing on the GitHub remote (check locally tests first to give CI time to run). You may start implementation/review loops as needed to deal with issues.</p></li><li><p>After each subtitle section, also run <code>EpiAwarePackageTools.update(pkgdir(EpiSewer))</code> to re-apply the managed standard and report drift against the current kit, before dispatching the review agent.</p></li><li><p>Review agents should make sure to check against <code>EpiAwarePackageTools.jl</code> enforced as well as written (i.e. in the documentation).</p></li><li><p>You should make sure to make a todo list to do your manage the next step, then check this document again. Updating/clearing the todo list each time.</p></li><li><p>As a guide we are aiming to highlight the <code>ComposableTuringIDModels.jl</code> ecosystem so we are aiming for full coverage of the model implemented here but with as few new components as possible i.e. we wish to reuse as much of our current ecosystem as we can.</p></li><li><p>Note the style/names/interface is all drawn from <code>ComposableTuringIDModels.jl</code> and should be modular so i.e no <code>EpiSewer</code> wrapper function no none <code>Struct</code> interface elements or wrappers over multipe modules.</p></li><li><p>It is important this work is async - don&#39;t try and access folders out of this one or take destructive actions recursively.</p></li><li><p>If subagents stalled or fail feel free to restart those workers as needed.</p></li></ul>',48)])])}const u=t(s,[["render",n]]);export{m as __pageData,u as default};
