import{_ as t,o,c as i,an as a}from"./chunks/framework.DVHqmV-v.js";const m=JSON.parse('{"title":"Model components","description":"","frontmatter":{},"headers":[],"relativePath":"components/model-components.md","filePath":"components/model-components.md","lastUpdated":null}'),d={name:"components/model-components.md"};function n(s,e,r,c,l,h){return o(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="model-components" tabindex="-1">Model components <a class="header-anchor" href="#model-components" aria-label="Permalink to &quot;Model components {#model-components}&quot;">​</a></h1><p>The <a href="https://github.com/adrian-lison/EpiSewer" target="_blank" rel="noreferrer">EpiSewer</a> R package is a Bayesian generative model that estimates the effective reproduction number <code>R_t</code> and other transmission indicators from wastewater measurements. This package replicates that model functionality using composable components from the EpiAware ecosystem (<a href="https://epiaware.org/ComposableTuringIDModels.jl" target="_blank" rel="noreferrer"><code>ComposableTuringIDModels.jl</code></a> and <a href="https://epiaware.org/EpiAwareADTools.jl" target="_blank" rel="noreferrer"><code>EpiAwareADTools.jl</code></a>).</p><p>The original EpiSewer model is organised into six modules. The table below lists every model component, its role, and the ecosystem component that provides the same functionality, so a reader coming from the R package can see what has a counterpart here and what does not.</p><h2 id="Component-mapping" tabindex="-1">Component mapping <a class="header-anchor" href="#Component-mapping" aria-label="Permalink to &quot;Component mapping {#Component-mapping}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">EpiSewer module</th><th style="text-align:right;">EpiSewer component (R name)</th><th style="text-align:right;">Role in model</th><th style="text-align:right;">Provided by</th></tr></thead><tbody><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>concentrations_observe</code></td><td style="text-align:right;">Score the concentration measurements against the expected concentration</td><td style="text-align:right;">An observation error model wrapping the latent expected concentration (<code>NormalError</code>, <code>LogNormalError</code>)</td></tr><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>concentrations_observe_partitions</code></td><td style="text-align:right;">Score the positive dPCR partition counts with a binomial likelihood instead of the concentration</td><td style="text-align:right;"><code>DigitalPCRError</code> in <code>src/measurements.jl</code>, a <code>TransformObservationModel</code> applying the Poisson partition law <code>p_t = 1 - exp(-exp(Y_t))</code> over a <code>BinomialError</code></td></tr><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>noise_estimate</code></td><td style="text-align:right;">Measurement noise as a constant coefficient of variation</td><td style="text-align:right;"><code>GammaError</code> in <code>src/measurements.jl</code>, matching R&#39;s <code>gamma3_lpdf</code> shape <code>1/cv²</code> and rate <code>1/(Y_t cv²)</code>. <code>LogNormalError</code> is R&#39;s log-normal option, same relative-noise contract</td></tr><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>noise_estimate_constant_var</code></td><td style="text-align:right;">Measurement noise as a constant variance</td><td style="text-align:right;"><code>NormalError</code> with a shared <code>std</code> prior</td></tr><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>noise_estimate_dPCR</code> / <code>noise_estimate_dPCR_params</code></td><td style="text-align:right;">Concentration-dependent CV derived from the dPCR partition model, predicting more relative variation at low concentrations</td><td style="text-align:right;">No ecosystem counterpart; this needs a CV that is a function of the expected concentration</td></tr><tr><td style="text-align:right;">measurements</td><td style="text-align:right;"><code>LOD_assume</code> / <code>LOD_estimate_dPCR</code></td><td style="text-align:right;">Left-censoring of measurements at the detection limit</td><td style="text-align:right;"><code>LOD</code> in <code>src/measurements.jl</code>, an <code>AbstractObservationErrorModel</code> wrapping an inner error distribution in <code>Distributions.censored</code>, with values at the limit scored as a <code>logcdf</code>. The limit is assumed rather than derived from the dPCR model</td></tr><tr><td style="text-align:right;">sampling</td><td style="text-align:right;"><code>outliers_estimate</code></td><td style="text-align:right;">Integrated outlier detection for measurements</td><td style="text-align:right;"><code>MeasurementOutliers</code> in <code>src/sampling.jl</code>, an <code>Ascertainment</code> over <code>IID(truncated(GeneralizedExtremeValue(0, 2e-8, 4), 0, Inf))</code> with an additive transform, adding <code>scale · ε_t</code> to the expected concentration</td></tr><tr><td style="text-align:right;">sampling</td><td style="text-align:right;"><code>sample_effects_estimate_weekday</code> / <code>sample_effects_estimate_matrix</code></td><td style="text-align:right;">Log-linear regression of concentration on sample covariates (weekday, age of sample)</td><td style="text-align:right;"><code>ascertainment_dayofweek</code> for the weekday case; the general design-matrix regression has no ecosystem counterpart</td></tr><tr><td style="text-align:right;">sewage</td><td style="text-align:right;"><code>flows_observe</code> / <code>flows_assume</code></td><td style="text-align:right;">Flow-normalise the shed load into a concentration</td><td style="text-align:right;"><code>FlowNormalize</code> in <code>src/sewage.jl</code>, which divides the expected <strong>load</strong> by the daily flow and then delegates to the wrapped observation model. The flow is data, passed as <code>y_t = (y = concentrations, flow = flow)</code></td></tr><tr><td style="text-align:right;">sewage</td><td style="text-align:right;"><code>residence_dist_assume</code></td><td style="text-align:right;">Convolve the shedding signal through a sewer residence-time distribution</td><td style="text-align:right;"><code>LatentDelay</code>, via <code>model()</code>&#39;s <code>residence_dist</code></td></tr><tr><td style="text-align:right;">shedding</td><td style="text-align:right;"><code>incubation_dist_assume</code></td><td style="text-align:right;">Incubation period, so a shedding profile indexed from symptom onset can be applied to infections</td><td style="text-align:right;"><code>LatentDelay</code>, via <code>model()</code>&#39;s <code>incubation_dist</code></td></tr><tr><td style="text-align:right;">shedding</td><td style="text-align:right;"><code>shedding_dist_assume</code> / <code>shedding_dist_estimate</code></td><td style="text-align:right;">Shedding load profile, assumed or inferred</td><td style="text-align:right;"><code>LatentDelay</code> with the shedding load distribution, or with an <code>UncertainDelay</code> when its parameters carry priors</td></tr><tr><td style="text-align:right;">shedding</td><td style="text-align:right;"><code>load_per_case_assume</code> / <code>load_per_case_calibrate</code></td><td style="text-align:right;">Load shed per case</td><td style="text-align:right;"><code>Ascertainment</code> over a <code>FixedIntercept</code>, scaling <code>I_t</code> at the observation stage</td></tr><tr><td style="text-align:right;">shedding</td><td style="text-align:right;"><code>load_variation_estimate</code></td><td style="text-align:right;">Individual-level shedding-load overdispersion</td><td style="text-align:right;"><code>LoadVariation</code> in <code>src/shedding.jl</code>, in the default chain outside the load-per-case step. R&#39;s <code>gamma_sum_log_approx</code>: a normal draw at the gamma&#39;s moments, floored by a softplus</td></tr><tr><td style="text-align:right;">infections</td><td style="text-align:right;"><code>generation_dist_assume</code></td><td style="text-align:right;">Generation-time distribution between infections</td><td style="text-align:right;"><code>Renewal(generation_time = ...)</code>, which discretises a continuous distribution itself and drops the lag-0 bin</td></tr><tr><td style="text-align:right;">infections</td><td style="text-align:right;"><code>R_estimate_rw</code>, <code>R_estimate_gp</code> and six further smoothers</td><td style="text-align:right;">Flexible <code>R_t</code> smoothing</td><td style="text-align:right;"><code>RandomWalk</code> for the random walk and <code>HilbertSpaceGP</code> / <code>ExactGP</code> for the GP, both reachable through <code>model()</code>&#39;s <code>rt</code>, with <code>CombineLatentModels</code> summing GP terms and <code>TransformLatentModel</code> applying a link. <code>AR</code>, <code>MA</code> and <code>DiffLatentModel</code> are the nearest analogues of the spline, piecewise, exponential-smoothing and smooth-derivative options</td></tr><tr><td style="text-align:right;">infections</td><td style="text-align:right;"><code>seeding_estimate_constant</code> / <code>seeding_estimate_growth</code> / <code>seeding_estimate_rw</code></td><td style="text-align:right;">Infections over the seeding phase, whose length is the generation interval&#39;s horizon, before the renewal recursion can be applied</td><td style="text-align:right;"><code>Renewal</code>&#39;s <code>initialisation</code>, set through <code>model()</code>&#39;s <code>seeding</code>, seeds the window at <code>I₀</code> decaying at the growth rate implied by <code>R₀</code>, a fixed exponential. That covers the intercept of R&#39;s random walk over the seeding phase; the walk itself needs a custom renewal step whose <code>recurrent_step</code> overrides <code>renewal_init_window</code></td></tr><tr><td style="text-align:right;">infections</td><td style="text-align:right;"><code>infection_noise_estimate</code></td><td style="text-align:right;">Stochastic infections, negative-binomial around the renewal expectation</td><td style="text-align:right;"><code>InfectionNoise</code> in <code>src/infections.jl</code>, an <code>AbstractRenewalModifier</code> whose <code>apply_modifier</code> returns a non-centred draw with the negative-binomial variance at the renewal expectation, which is the incidence the recursion then carries forward</td></tr><tr><td style="text-align:right;">forecast</td><td style="text-align:right;"><code>horizon_assume</code></td><td style="text-align:right;">Probabilistic forecast of <code>R_t</code>, infections and concentrations</td><td style="text-align:right;"><code>forecast</code>, which extends each draw&#39;s latent innovations over the horizon and predicts through the observation model</td></tr><tr><td style="text-align:right;">forecast</td><td style="text-align:right;"><code>damping_assume</code></td><td style="text-align:right;">Exponential damping of the forecast <code>R_t</code> trend, so extrapolated transmission levels off</td><td style="text-align:right;">No ecosystem counterpart</td></tr></tbody></table><p>R&#39;s <code>noise_estimate</code> offers four distribution families for the concentration likelihood: gamma by default, then log-normal, truncated normal and normal. <code>GammaError</code> is the default and <code>LogNormalError</code> is the log-normal option.</p><h2 id="Composition" tabindex="-1">Composition <a class="header-anchor" href="#Composition" aria-label="Permalink to &quot;Composition {#Composition}&quot;">​</a></h2><p>Most of the wastewater model is existing <code>ComposableTuringIDModels.jl</code> components. The infection process (<code>Renewal</code>), <code>R_t</code> smoothing (<code>RandomWalk</code>, <code>HilbertSpaceGP</code>), discrete delays (<code>LatentDelay</code>) and observation error (<code>NormalError</code>, <code>NegativeBinomialError</code>) all come from there.</p><p>Two of the wastewater-specific pieces are compositions with no new struct at all. Load-per-case calibration is an <code>Ascertainment</code> scaling <code>I_t</code> by the load shed per case. R calibrates that value once outside the sampler and passes it as data, so <code>model()</code> takes a number and wraps it in a <code>FixedIntercept</code>. Passing a prior instead infers it. The sewer residence time is a <code>LatentDelay</code>, and R&#39;s default <code>residence_dist = c(1)</code> is a point mass at same-day arrival, so an identity convolution.</p><p>This package adds seven <code>ComposableTuringIDModels.jl</code>-compatible structs. Three are compositions of ecosystem pieces:</p><ul><li><p><code>MeasurementOutliers</code> is an <code>Ascertainment</code> over <code>IID(truncated(GEV(...)))</code> with an additive transform.</p></li><li><p><code>DigitalPCRError</code> is a <code>TransformObservationModel</code> applying the cloglog-inverse link over a <code>BinomialError</code>.</p></li><li><p><code>LOD</code> wraps an inner error model&#39;s distribution in <code>Distributions.censored</code>.</p></li></ul><p>Four provide behaviour the ecosystem does not. <code>FlowNormalize</code> reads the daily flow out of the observation-data contract. <code>LogNormalError</code> is a relative-noise error family, parameterised by a coefficient of variation. <code>InfectionNoise</code> is a renewal modifier, drawing infections with the negative-binomial variance at the renewal expectation and feeding the draw forward through the scan. <code>LoadVariation</code> draws the realised shedding load around the expected one, with the variance of a sum of individual loads.</p><p><code>InfectionNoise</code> resolves to an <code>InfectionNoiseDraws</code> once its standard normals are drawn, which is the form the renewal scan steps through.</p><h2 id="The-default-chain-and-what-sits-outside-it" tabindex="-1">The default chain and what sits outside it <a class="header-anchor" href="#The-default-chain-and-what-sits-outside-it" aria-label="Permalink to &quot;The default chain and what sits outside it {#The-default-chain-and-what-sits-outside-it}&quot;">​</a></h2><p><code>EpiSewer.model()</code> composes a <code>Renewal</code> infection process, with a summed Gaussian-process <code>R_t</code> prior and stochastic infections, observed through the chain incubation delay → individual-level load variation → per-case load → shedding delay → flow division → outlier spikes → log-normal noise. The <a href="/EpiSewer.jl/dev/getting-started/index#getting-started">getting started</a> page reads both stages off the assembled model and shows how to replace either.</p><p>One part of R&#39;s model tree sits outside that assembly.</p><p><strong>The random walk over the seeding phase.</strong> R&#39;s <code>seeding_estimate_rw</code> runs a random walk on log infections across the seeding phase, whose length is the generation interval&#39;s horizon. <code>model()</code>&#39;s <code>seeding</code> supplies the intercept of that walk, and <code>Renewal</code> seeds the window at <code>I₀</code> decaying at the rate implied by <code>R₀</code>. A walk there is a custom renewal step whose <code>recurrent_step</code> overrides <code>renewal_init_window</code>.</p><p>R&#39;s <code>R_estimate_gp</code> sums two Matérn-3/2 GPs, a short-term one at 21 ± 3.5 days with magnitude 0.125 and a long-term one at 84 ± 7 days with magnitude 0.25, then maps the sum through an <code>inv_softplus</code> link. The default here is the same sum, built by <code>CombineLatentModels</code> and mapped through <code>softplus_link</code> with <code>TransformLatentModel</code>. The link is unbounded above; R&#39;s <code>R_max</code> applies only to its alternative <code>scaled_logit</code> link. A <code>HilbertSpaceGP</code> measures its length scale in standard deviations of the standardised time index rather than in days, so <code>gp_length_scale</code> converts R&#39;s prior in days at a given series length and <code>model()</code>&#39;s <code>n_gp</code> sets the length the default assumes.</p><p><code>residence_dist_assume</code> needs no wiring either way. R&#39;s default <code>residence_dist = c(1)</code> is a point mass at same-day arrival, so <code>model()</code>&#39;s <code>residence_dist</code> defaults to <code>nothing</code> and the wrapper is absent. Pass <code>residence_dist</code> for a non-trivial residence time.</p><h2 id="Discretisation" tabindex="-1">Discretisation <a class="header-anchor" href="#Discretisation" aria-label="Permalink to &quot;Discretisation {#Discretisation}&quot;">​</a></h2><p>EpiSewer ships its own discretisation helpers (<code>get_discrete_gamma</code> and friends), which assign the mass beyond a right-truncation point to the last bin. Here, delay inputs are handed to <code>Renewal</code> and <code>LatentDelay</code> as continuous distributions and those components discretise them. They apply primary uniform-window censoring, right-truncation at the horizon <code>D</code>, then daily interval censoring, and evaluate the result on the bin left edges before renormalising. The discretisation itself lives in <code>ComposableTuringIDModels.jl</code>, which calls <a href="https://epiaware.org/CensoredDistributions.jl" target="_blank" rel="noreferrer"><code>CensoredDistributions.jl</code></a>&#39;s <code>double_interval_censored</code>. This package does not depend on <code>CensoredDistributions.jl</code> directly.</p><p>One discretisation path means one place for the conventions to live. A generation interval has no mass at lag 0, which <code>Renewal</code> enforces by dropping that bin and renormalising. The default generation time is a location-shifted <code>Gamma</code> with no mass below lag 1, so that step removes nothing and the renormalisation is exact.</p><p>The same keyword arguments also accept an already discretised PMF vector, or an <code>UncertainDelay</code> whose distribution parameters carry priors, in which case the delay is inferred rather than fixed. A PMF vector is used verbatim, so a caller passing one owns the no-same-day-transmission convention themselves.</p>',23)])])}const p=t(d,[["render",n]]);export{m as __pageData,p as default};
